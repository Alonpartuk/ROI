# Octup Dashboard Frontend - Environment Variables
# =================================================
# Copy this file to .env and fill in your values

# API Configuration
# The backend server URL (Express server that connects to BigQuery)
REACT_APP_API_URL=http://localhost:3001/api

# Data Source (api = real BigQuery, mock = demo data)
# Set to 'mock' if you want to run the frontend without the backend
REACT_APP_DATA_SOURCE=api
